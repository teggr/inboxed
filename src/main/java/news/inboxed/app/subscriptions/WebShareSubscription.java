package news.inboxed.app.subscriptions;


public record WebShareSubscription(   ) {

  // push the inbox data?
  // receive a notification. new event available
  // last updated (pointer) -> used for query (all last updated - need to add this date)
  // 

}
